# Variables

Variables store values.

In Splice, variables are created using `let`.

Example:

    let x = 10
    let name = "Splice"

You can use variables in expressions.

Example:

    let a = 5
    let b = 7

    print(a + b)

Output:

    12
    